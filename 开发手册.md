# 异界编年史 · 开发手册

> 面向开发者的代码结构、算法逻辑与组件详解

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [目录结构](#2-目录结构)
3. [部署层：Docker + Nginx](#3-部署层docker--nginx)
4. [后端：FastAPI 应用](#4-后端fastapi-应用)
   - 4.1 [启动与生命周期](#41-启动与生命周期)
   - 4.2 [配置管理](#42-配置管理)
   - 4.3 [数据库与 ORM](#43-数据库与-orm)
   - 4.4 [认证系统](#44-认证系统)
   - 4.5 [存档 API](#45-存档-api)
   - 4.6 [AI 对话接口](#46-ai-对话接口)
   - 4.7 [RAG 世界记忆系统](#47-rag-世界记忆系统)
   - 4.8 [文生图接口](#48-文生图接口)
   - 4.9 [访问统计接口](#49-访问统计接口)
5. [前端：单文件 HTML 应用](#5-前端单文件-html-应用)
   - 5.1 [页面状态机](#51-页面状态机)
   - 5.2 [游戏状态对象 gs](#52-游戏状态对象-gs)
   - 5.3 [Structured Output 解析](#53-structured-output-解析)
   - 5.4 [SSE 流式渲染与打字动画](#54-sse-流式渲染与打字动画)
   - 5.5 [历史压缩算法](#55-历史压缩算法)
   - 5.6 [特殊事件机制](#56-特殊事件机制)
   - 5.7 [图片并行生成](#57-图片并行生成)
   - 5.8 [存档自动保存](#58-存档自动保存)
6. [数据库 Schema](#6-数据库-schema)
7. [API 接口速查表](#7-api-接口速查表)
8. [关键算法深度解析](#8-关键算法深度解析)
9. [扩展与修改指南](#9-扩展与修改指南)

---

## 1. 系统架构总览

```
浏览器
  │
  │ HTTP (port 8080)
  ▼
┌─────────────────────────────────────┐
│            Nginx (Alpine)           │
│  /auth/ /game/ /ai/ → 反向代理      │
│  /docs /openapi.json → 反向代理      │
│  /         → 静态文件 (frontend/)   │
└──────────────┬──────────────────────┘
               │ HTTP (port 8000, 内网)
               ▼
┌─────────────────────────────────────┐
│         FastAPI (Uvicorn)           │
│  routers/auth.py   → 用户认证        │
│  routers/game.py   → 存档 CRUD      │
│  routers/facts.py  → RAG 事实存储   │
│  routers/ai.py     → AI 对话 SSE    │
│  routers/image.py  → 文生图任务     │
└──────────────┬──────────────────────┘
               │ SQLAlchemy (TCP 5432, 内网)
               ▼
┌─────────────────────────────────────┐
│     PostgreSQL 16 + pgvector        │
│  users         → 用户账号           │
│  game_saves    → 存档数据           │
│  world_facts   → 向量化剧情事实     │
└─────────────────────────────────────┘

外部 API：
  DashScope qwen-plus        → AI 对话 (SSE)
  DashScope text-embedding-v3 → 文本向量化
  DashScope wanx2.1-t2i-turbo → 文生图
```

**数据流向（一次对话）**：

```
用户输入
  → 前端拼装 system_prompt + messages
  → POST /ai/chat
  → 后端查 RAG（相关事实）注入 system_prompt
  → 调用 Qwen SSE 流
  → 前端逐字渲染
  → 解析 <<<JSON>>> 结构化输出
  → 应用 commands（HP/属性变化）
  → autoSave() 持久化
  → storeFact() 向量化存储新事实
```

---

## 2. 目录结构

```
ai-world-game/
├── docker-compose.yml          # 三服务编排：db / backend / nginx
├── .env.example                # 根级 env 模板（DB_PASSWORD）
├── .gitignore
├── DEVLOG.md                   # 版本迭代日志
├── 使用手册.md
├── 开发手册.md
│
├── nginx/
│   └── nginx.conf              # 反向代理规则 + 静态服务
│
├── frontend/
│   └── index.html              # 整个前端：HTML + CSS + JS 单文件
│
└── backend/
    ├── Dockerfile
    ├── requirements.txt
    ├── .env.example            # 后端 env 模板
    ├── main.py                 # FastAPI 入口，注册路由
    ├── config.py               # 读取环境变量
    ├── database.py             # SQLAlchemy engine + session
    ├── models.py               # ORM 模型
    ├── schemas.py              # Pydantic 请求/响应模型
    ├── security.py             # JWT + bcrypt 认证
    ├── embedding.py            # DashScope 向量化封装
    ├── alembic.ini
    ├── alembic/
    │   ├── env.py
    │   └── versions/           # 数据库迁移脚本
    └── routers/
        ├── auth.py             # 注册/登录/用户信息
        ├── game.py             # 存档 CRUD
        ├── facts.py            # RAG 事实存储与查询
        ├── ai.py               # AI 对话（SSE 流）
        └── image.py            # 文生图（异步任务）
```

---

## 3. 部署层：Docker + Nginx

### docker-compose.yml 要点

```yaml
services:
  db:
    image: pgvector/pgvector:pg16   # 包含向量扩展的 PostgreSQL
    healthcheck:                    # backend 等 db 健康才启动
      test: pg_isready -U postgres -d aiworld

  backend:
    build: ./backend
    env_file: ./backend/.env        # 读取本地 .env
    environment:
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@db:5432/aiworld
      # 注意：environment 中的值会覆盖 env_file 中同名项
      # DB_PASSWORD 来自根目录 .env

  nginx:
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro  # 挂载配置
      - ./frontend:/usr/share/nginx/html:ro                   # 挂载前端
```

**为什么不把 nginx.conf 挂到 `/etc/nginx/nginx.conf`？**
因为 nginx 镜像的主配置用 `include /etc/nginx/conf.d/*.conf`，直接替换主配置会丢失全局 mime types 等设置导致静态文件 Content-Type 错误。

### nginx.conf 路由规则

```nginx
# 精确匹配 API 路径前缀 → 代理到后端
location /auth/  { proxy_pass http://backend; }
location /game/  { proxy_pass http://backend; }
location /ai/    {
    proxy_pass http://backend;
    proxy_buffering off;     # 关键：SSE 流不能缓冲
    proxy_read_timeout 300s; # 图片生成最长等 5 分钟
}

# FastAPI 文档
location ~ ^/(docs|redoc|openapi.json) { proxy_pass http://backend; }

# 前端 SPA（所有未匹配路径返回 index.html）
location / { try_files $uri $uri/ /index.html; }
```

---

## 4. 后端：FastAPI 应用

### 4.1 启动与生命周期

```python
# main.py
@asynccontextmanager
async def lifespan(app: FastAPI):
    run_migrations()  # 启动时自动执行 alembic upgrade head
    yield            # 应用运行中
    # yield 之后可以放清理逻辑（当前没有）

app = FastAPI(lifespan=lifespan)
```

`run_migrations()` 在每次容器启动时跑 Alembic 迁移，保证数据库 schema 始终最新。生产环境这样做是安全的，因为 Alembic 是幂等的（已应用的迁移不会重复执行）。

### 4.2 配置管理

```python
# config.py
def _require(key: str) -> str:
    val = os.getenv(key)
    if not val:
        raise RuntimeError(f"Missing required env var: {key}")
    return val

DATABASE_URL = _require("DATABASE_URL")  # 缺失则启动失败，快速暴露配置问题
QWEN_API_KEY = _require("QWEN_API_KEY")
JWT_SECRET   = _require("JWT_SECRET")
```

`_require` 的设计：强制必填项在启动时报错，而不是在运行时才发现配置缺失。可选项用 `os.getenv(key, default)` 提供默认值。

### 4.3 数据库与 ORM

```python
# database.py
engine = create_engine(DATABASE_URL)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

def get_db():          # FastAPI 依赖注入用的生成器
    db = SessionLocal()
    try:
        yield db       # 请求处理完成后自动关闭 session
    finally:
        db.close()
```

路由函数通过 `db: Session = Depends(get_db)` 注入 session，FastAPI 会在请求结束后自动调用 `finally` 关闭连接，不需要手动管理。

### 4.4 认证系统

**密码存储**（`security.py`）：

```python
import bcrypt

def hash_password(password: str) -> str:
    return bcrypt.hashpw(password.encode(), bcrypt.gensalt()).decode()

def verify_password(plain: str, hashed: str) -> bool:
    return bcrypt.checkpw(plain.encode(), hashed.encode())
```

bcrypt 的核心特性：
- `gensalt()` 每次生成不同的盐值，同一密码每次 hash 结果不同
- 内置慢哈希（cost factor），对抗暴力破解
- 之前用 `passlib` 封装，因 `passlib` 停止维护与 `bcrypt>=4.0` 不兼容，已改为直接调用

**JWT Token**：

```python
def create_token(user_id: str) -> str:
    expire = datetime.utcnow() + timedelta(minutes=JWT_EXPIRE_MINUTES)  # 30天
    return jwt.encode({"sub": user_id, "exp": expire}, JWT_SECRET, algorithm="HS256")
```

**可选认证**（`get_optional_user`）：

```python
oauth2_scheme_optional = OAuth2PasswordBearer(tokenUrl="/auth/login", auto_error=False)

def get_optional_user(token = Depends(oauth2_scheme_optional), ...) -> Optional[User]:
    if not token: return None          # 未登录 → 游客
    try:
        payload = jwt.decode(...)
        ...
    except JWTError: return None       # token 无效 → 视为游客，不报错
    return db.query(User).filter(...).first()
```

AI 对话和图片接口用 `get_optional_user`，登录和不登录都能用，游客只是没有存档功能。存档 CRUD 用 `get_current_user`，token 无效则返回 401。

### 4.5 存档 API

```python
# routers/game.py
@router.put("/saves/{save_id}")
def update_save(save_id, body: SaveUpdate, ...):
    save = db.query(GameSave).filter(
        GameSave.id == save_id,
        GameSave.user_id == user.id    # 必须是本人的存档，防止越权
    ).first()
    for key, val in body.model_dump(exclude_none=True).items():
        setattr(save, key, val)        # 只更新非 None 的字段
    db.commit()
```

`exclude_none=True` 的作用：前端只传需要更新的字段，其余字段保持原值，避免误覆盖。

### 4.6 AI 对话接口

```python
# routers/ai.py
@router.post("/chat")
async def chat(body: AIRequest, db, user):
    system_prompt = body.system_prompt

    # RAG 注入：将相关世界记忆追加到 system prompt
    if body.save_id and body.messages:
        last_msg = body.messages[-1].content
        facts = await query_relevant_facts(body.save_id, last_msg, db)
        if facts:
            recall = "\n".join(f"- {f}" for f in facts)
            system_prompt += f"\n\n【世界记忆·相关事实】\n{recall}"

    messages = [{"role": "system", "content": system_prompt}]
    messages += [m.model_dump() for m in body.messages]

    async def stream():
        async with httpx.AsyncClient(timeout=60) as client:
            async with client.stream("POST", QWEN_URL, ...) as resp:
                async for line in resp.aiter_lines():
                    if line.startswith("data: "):
                        yield line + "\n\n"   # 透传 SSE 事件

    return StreamingResponse(stream(), media_type="text/event-stream")
```

后端是一个**SSE 透传代理**：接收 Qwen 的 SSE 流，原样转发给浏览器。这样前端无需直接持有 API 密钥，所有鉴权在后端完成。

### 4.7 RAG 世界记忆系统

**RAG（Retrieval-Augmented Generation）** 的作用：当游戏历史过长时，AI 容易"忘记"早期发生的重要事件。RAG 通过向量相似度搜索，将最相关的历史事实注入每次请求的 system prompt。

**存储流程**（前端每回合调用）：

```
GM 叙事文本
  → POST /game/saves/{id}/facts
  → get_embedding(text)             # 调用 text-embedding-v3，维度 1024
  → 存入 world_facts 表（text + vector）
```

**查询流程**（每次 AI 对话前）：

```python
# facts.py
async def query_relevant_facts(save_id, query, db, top_k=5):
    query_vec = await get_embedding(query)  # 将用户输入向量化
    rows = db.execute(text("""
        SELECT content FROM world_facts
        WHERE save_id = :save_id
        ORDER BY embedding <=> CAST(:vec AS vector)  -- pgvector 余弦距离
        LIMIT :k
    """), {...}).fetchall()
    return [r[0] for r in rows]
```

`<=>` 是 pgvector 的**余弦距离**运算符，距离越小表示语义越相近。`ORDER BY embedding <=> query_vector LIMIT 5` 取出最相关的 5 条记忆。

**为什么用 1024 维？**
DashScope `text-embedding-v3` 支持可配置维度（256/512/1024），1024 维语义表达能力最强，pgvector 索引性能仍可接受。

### 4.8 文生图接口

**异步任务模式**（避免前端长连接）：

```python
# routers/image.py
_tasks: dict[str, dict] = {}   # 内存任务池：{task_id: {status, url}}

@router.post("/image/start")
async def start_image(body):
    task_id = str(uuid.uuid4())
    _tasks[task_id] = {"status": "pending", "url": None}
    asyncio.create_task(_run_task(task_id, prompt))  # 后台异步跑
    return {"task_id": task_id}                       # 立即返回

@router.get("/image/{task_id}")
async def get_image_result(task_id):
    return _tasks.get(task_id)    # 前端轮询此接口

async def _run_task(task_id, prompt):
    # 1. 提交到 wanx（X-DashScope-Async: enable → 立即返回 wanx_task_id）
    # 2. 每 2s 查询一次任务状态
    # 3. SUCCEEDED → 更新 _tasks[task_id] = {status: "done", url: ...}
    # 4. FAILED/超时 → {status: "failed"}
```

**内存任务池的风险与处理**：
- 任务数量超过 200 时清理最旧的 100 个，避免内存无限增长
- 容器重启后所有 pending 任务丢失（前端会超时后自动清除 imgWrap）

---

### 4.9 访问统计接口

```python
# routers/stats.py
_tasks 内存池不涉及此模块，数据直接写 PostgreSQL

@router.post("/visit", status_code=201)
def record_visit(request: Request, db):
    db.add(PageVisit())   # 只记录时间戳，不存 IP（隐私友好）
    db.commit()
    return {}

@router.get("")
@router.get("/")          # 同时匹配 /stats 和 /stats/（nginx 会 301 加斜杠）
def get_stats(db):
    total      = db.query(func.count(PageVisit.id)).scalar()
    today      = db.query(func.count(...)).filter(date == today).scalar()
    daily      = db.query(date, count).group_by(date).order_by(date).limit(7)
    return {"total": total, "today": today_count, "daily": [...]}
```

**数据库表**：

```sql
CREATE TABLE page_visits (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX ix_page_visits_created_at ON page_visits(created_at);
```

只存时间戳，不存 IP 或 User-Agent，隐私无负担。按日期聚合查询利用索引，百万级数据下依然快速。

**前端上报**：

```javascript
// DOMContentLoaded 时静默上报
fetch(API_BASE + '/stats/visit', { method: 'POST' }).catch(() => {})
fetch(API_BASE + '/stats').then(r => r.json()).then(d => {
    document.getElementById('visit-count').textContent =
        `累计访问 ${d.total} 次 · 今日 ${d.today} 次`
}).catch(() => {})
```

两个 `.catch(() => {})` 保证统计失败不影响任何游戏功能。

---

## 5. 前端：单文件 HTML 应用

整个前端是一个 `index.html`，约 2200 行，包含 HTML 结构、CSS 样式和 JavaScript 逻辑。无构建工具，直接被 Nginx 静态服务。

### 5.1 页面状态机

前端有 4 个主要"页面"，通过 `display` 切换：

```
auth-panel（登录注册）
    ↓ 登录成功 / 点击游客
gen-panel（世界生成向导）
    ↓ 点击踏入异界
setup-panel（角色属性分配）
    ↓ 点击开始游戏
game-panel（主游戏界面）
    ↓ token 过期且不在游戏中
auth-panel（重新登录）
```

切换逻辑示例：
```javascript
// 进入游戏
document.getElementById('setup-panel').style.display = 'none'
document.getElementById('game-panel').style.display = 'block'

// 全局 401 处理（apiFetch 中）
const gameActive = document.getElementById('game-panel').style.display !== 'none'
if (gameActive) {
    // 游戏中 → 转游客模式，不跳转
    guestMode = true
} else {
    // 非游戏中 → 跳回登录页
    document.getElementById('auth-panel').style.display = 'block'
}
```

### 5.2 游戏状态对象 gs

`gs` 是游戏的核心状态，所有界面渲染和 AI 请求都基于它：

```javascript
gs = {
    worldBg: "...",           // 世界背景文本
    charName: "苦艾",         // 角色名
    charBg: "...",            // 角色背景
    hp: 100, maxHp: 100,      // 当前/最大生命值
    stats: { str:8, int:6, cha:5, agi:6 },  // 四项属性
    turn: 0,                  // 当前回合数
    history: [                // AI 对话历史（最多保留最近14条）
        { role: "user", content: "..." },
        { role: "assistant", content: "...<<<JSON>>>..." }
    ],
    summary: "",              // 历史压缩摘要（早期剧情）
    tone: "dark",             // 叙事风格
    nextEventTurn: 5,         // 下一次特殊事件触发回合
    saveId: "uuid...",        // 当前存档 ID（null = 未保存）
    saveName: "主线存档"      // 存档名
}
```

`gs` 只存在内存中，页面刷新丢失。游戏数据通过 `autoSave()` 持久化到后端，读档时从后端拉回填充 `gs`。

### 5.3 Structured Output 解析

**问题**：Qwen 模型输出是自由文本 + 结构化数据的混合，需要可靠地分离。

**方案**：用自定义分隔符 `<<<JSON>>>...<<<END>>>` 而不是纯 JSON 解析，避免叙事文本中偶然出现 `{` `}` 导致解析错误。

**System Prompt 约束**（每次请求都注入）：

```
叙事正文（3-5句）

<<<JSON>>>
{"choices":["选项A","选项B","选项C"],"commands":{"HP":-10}}
<<<END>>>
```

**前端解析**：

```javascript
function getVisible(raw) {
    // 截取 <<<JSON>>> 之前的内容 = 叙事正文
    const cut = raw.indexOf('<<<JSON>>>')
    return (cut === -1 ? raw : raw.slice(0, cut)).trim()
}

function parseStructuredResponse(raw) {
    const m = raw.match(/<<<JSON>>>([\s\S]*?)<<<END>>>/)
    if (!m) return { choices: [], commands: {} }
    try { return JSON.parse(m[1].trim()) }
    catch { return { choices: [], commands: {} } }
}
```

**commands 字段说明**：

```json
{
  "choices": ["向废墟走去", "原路返回", "原地观察"],
  "commands": {
    "HP": -10,          // HP 变化量（负=扣血，正=回血，0可省略）
    "STR": 1,           // 属性变化量
    "ending": {         // 可选：触发结局
      "type": "victory",
      "desc": "终于找到了妹妹"
    }
  }
}
```

### 5.4 SSE 流式渲染与打字动画

```
Qwen SSE → 后端透传 → 前端 ReadableStream → charBuffer → setInterval 打字
```

**核心实现**：

```javascript
let charBuffer = []   // 待打印的字符队列
let fullText = ''     // 完整累积文本（含 JSON 块）
let streamDone = false

// 1. 读取 SSE 流，写入 charBuffer
const reader = res.body.getReader()
while (true) {
    const { done, value } = await reader.read()
    if (done) break
    for (const line of decoder.decode(value).split('\n')) {
        if (line.startsWith('data: ') && line !== 'data: [DONE]') {
            const delta = JSON.parse(line.slice(6)).choices?.[0]?.delta?.content || ''
            fullText += delta
            // 只把叙事正文（<<<JSON>>> 之前）放入打字队列
            if (fullText.indexOf('<<<JSON>>>') === -1) {
                charBuffer.push(...delta.split(''))
            }
        }
    }
    if (done) streamDone = true
}

// 2. 定时器，每 20ms 打一个字
const typingTimer = setInterval(() => {
    if (charBuffer.length > 0) {
        typingDiv.textContent += charBuffer.shift()
        storyArea.scrollTop = 999999
    }
    if (streamDone && charBuffer.length === 0) {
        clearInterval(typingTimer)
        finishNarrative()   // 流结束且字符打完 → 解析 JSON、显示选项
    }
}, 20)

// 3. 点击跳过
storyArea.addEventListener('click', () => {
    charBuffer = []
    typingDiv.textContent = getVisible(fullText)  // 直接显示完整文本
    if (streamDone) finishNarrative()
}, { once: true })
```

**注意**：`charBuffer` 只包含叙事正文的字符，JSON 块不打印出来（`getVisible` 截断）。但 `fullText` 保留完整内容用于后续解析。

### 5.5 历史压缩算法

**问题**：Qwen 有 context window 限制，游戏玩太久历史会超长，且影响速度和成本。

**触发条件**：`gs.history.length > 28`

**算法**：

```javascript
async function compressHistory() {
    const toCompress = gs.history.slice(0, -14)   // 保留最近14条不压缩
    if (toCompress.length < 6) return              // 太少没必要压缩

    // 将旧消息发给 AI 总结
    const messages = [
        ...toCompress,
        { role: 'user', content: '请用3-5句话总结以上剧情的关键事件...' }
    ]
    const res = await fetch('/ai/chat', {
        body: JSON.stringify({
            system_prompt: `你是故事总结助手。世界：${gs.worldBg.slice(0,100)}。角色：${gs.charName}。`,
            messages
        })
    })
    // 读取 SSE 流，收集完整摘要文本
    let summary = ''
    // ... 解析 SSE ...
    if (summary.trim()) {
        gs.summary = (gs.summary ? gs.summary + '\n' : '') + summary.trim()
        gs.history = gs.history.slice(-14)   // 修剪历史到最近14条
    }
}
```

**效果**：

```
压缩前：history 有 30 条消息（每条约 200 字 = 6000 字）
压缩后：history 最近 14 条 + summary 约 100 字
       下次请求的 context 减少约 70%
```

**System Prompt 注入摘要**：

```javascript
const systemPrompt = `你是沉浸式文字RPG的GM。
...
${gs.summary ? `\n【早期剧情摘要】\n${gs.summary}\n` : ''}
...`
```

### 5.6 特殊事件机制

**随机间隔触发**（不固定回合，避免被玩家预测）：

```javascript
// 初始化时
gs.nextEventTurn = 3 + Math.floor(Math.random() * 3)  // 3-5 回合后首次触发

// callGM 开头
const eventTurn = !skipHistory && gs.turn >= gs.nextEventTurn
if (eventTurn) {
    // 设置下一次触发时间
    gs.nextEventTurn = gs.turn + 3 + Math.floor(Math.random() * 4)  // 再隔 3-6 回合
    addBlock('special-event', '✦ 意外降临 ✦')
}

// 将指令注入到用户消息
const finalMsg = eventTurn
    ? userMsg + '\n\n【GM指令：本回合必须引入一个意外事件——可以是突发遭遇、危机、发现或转机，要出乎意料且推动剧情】'
    : userMsg
```

**为什么注入用户消息而不是 System Prompt？**
System Prompt 是固定的，注入用户消息更像是"玩家行动的额外指令"，GM 优先级最高规则可以保证其执行，且每次只对当前回合有效。

### 5.7 图片并行生成

**核心思路**：图片生成耗时 20-40s，用户在读打字动画期间（5-10s）以及思考选项期间（若干秒），这段时间可以"白嫖"用来生成图片。

**时序图**：

```
t=0s   callGM 启动
t=0s   插入 imgWrap（shimmer占位）        │
t=0s   POST /ai/chat 发送请求            │ 图片生成
t=1s   SSE 开始流入，打字动画启动         │ 在后台跑
t=1s   fetchImage() 启动                │
t=1s   等待 fullText > 60 字            │
t=3s   拼合提示词，POST /ai/image/start  │
t=3s   立即得到 task_id（<100ms）        │
t=3s   开始每 2s 轮询 GET /ai/image/:id  │
t=8s   SSE 结束，打字动画播完            │
t=8s   显示选项，用户思考...             │
t=25s  wanx 生成完成                    │
t=25s  img.src = url                    │
t=25s  图片加载完成，替换 shimmer         ←
```

**只在特殊事件触发**：

```javascript
if (eventTurn && imgWrap) {
    fetchImage()   // 只有 eventTurn 才生成图片
}
```

**提示词构建**（世界观 + 当前场景）：

```javascript
const worldCtx = gs.worldBg.slice(0, 80).replace(/\n/g, ' ')
const fullPrompt = `${worldCtx}。当前场景：${visText}`
// 例："末日荒原，核冬天第三年，人类文明大部分崩溃。当前场景：废墟中弥漫着铁锈气味..."
```

### 5.8 存档自动保存

```javascript
async function autoSave() {
    if (!gs.worldBg || guestMode) return  // 游客不存档

    const payload = {
        save_name: gs.saveName || '自动存档',
        world_bg: gs.worldBg, char_name: gs.charName, ...
        history: gs.history.slice(-20)    // 只存最近20条，节省存储
    }

    if (gs.saveId) {
        await apiFetch('PUT', `/game/saves/${gs.saveId}`, payload)   // 更新
    } else {
        const saved = await apiFetch('POST', '/game/saves', payload)  // 首次创建
        gs.saveId = saved.id   // 记录 ID 供后续更新使用
    }
}
```

`autoSave` 在 `finishNarrative()` 末尾调用，即每次 GM 回复完成后自动触发。失败（网络错误、401）时打印到 console，不影响游戏。

---

## 6. 数据库 Schema

```sql
-- 用户表
CREATE TABLE users (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username    VARCHAR(50) UNIQUE NOT NULL,
    email       VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at  TIMESTAMPTZ DEFAULT now()
);

-- 存档表
CREATE TABLE game_saves (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id     UUID REFERENCES users(id) ON DELETE CASCADE,
    save_name   VARCHAR(100) DEFAULT '存档',
    world_bg    VARCHAR(2000) NOT NULL,
    char_name   VARCHAR(50) NOT NULL,
    char_bg     VARCHAR(500) DEFAULT '',
    hp          INTEGER DEFAULT 100,
    max_hp      INTEGER DEFAULT 100,
    stats       JSONB DEFAULT '{"str":5,"int":5,"cha":5,"agi":5}',
    turn        INTEGER DEFAULT 0,
    history     JSONB DEFAULT '[]',   -- 最近20条对话
    created_at  TIMESTAMPTZ DEFAULT now(),
    updated_at  TIMESTAMPTZ DEFAULT now()
);

-- 世界事实表（RAG 用）
CREATE TABLE world_facts (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    save_id     UUID REFERENCES game_saves(id) ON DELETE CASCADE,
    content     TEXT NOT NULL,             -- 原始叙事文本
    embedding   vector(1024),              -- text-embedding-v3 向量
    turn        INTEGER DEFAULT 0,
    created_at  TIMESTAMPTZ DEFAULT now()
);
```

**为什么 history 存 JSONB 而不是单独建表？**
- 对话历史作为一个整体被读取和写入，不需要单条查询
- JSONB 支持高效的整体更新
- 避免 N+1 查询问题

---

## 7. API 接口速查表

| 方法 | 路径 | 认证 | 描述 |
|------|------|------|------|
| POST | /auth/register | 无 | 注册账号 |
| POST | /auth/login | 无 | 登录，返回 JWT |
| GET | /auth/me | 必须 | 获取当前用户信息 |
| GET | /game/saves | 必须 | 列出所有存档 |
| POST | /game/saves | 必须 | 创建新存档 |
| GET | /game/saves/:id | 必须 | 获取存档详情 |
| PUT | /game/saves/:id | 必须 | 更新存档 |
| DELETE | /game/saves/:id | 必须 | 删除存档 |
| POST | /game/saves/:id/facts | 必须 | 存入事实向量 |
| POST | /ai/chat | 可选 | AI 对话（SSE 流） |
| POST | /ai/image/start | 可选 | 提交文生图任务，立即返回 task_id |
| GET | /ai/image/:task_id | 无 | 查询图片任务状态/结果 |
| POST | /stats/visit | 无 | 上报一次页面访问 |
| GET | /stats/ | 无 | 查询访问统计（总量/今日/每日） |

> 完整的请求/响应 schema 见 `http://localhost:8080/docs`

---

## 8. 关键算法深度解析

### 向量相似度搜索（RAG）

```
文本 → 模型编码 → 1024维向量
                         ↓
                   [0.12, -0.34, 0.89, ...]  ← 语义的数学表示

查询时：
用户输入向量 ←→ 历史事实向量
  余弦距离 = 1 - (A·B)/(|A|×|B|)
  距离越小 = 语义越相近 = 越相关
```

pgvector 的 `<=>` 运算符在数据库层直接完成向量计算，性能远高于在 Python 层循环计算。

### SSE（Server-Sent Events）协议

SSE 是单向的 HTTP 长连接，服务端持续推送数据：

```
HTTP/1.1 200 OK
Content-Type: text/event-stream

data: {"choices":[{"delta":{"content":"废"}}]}

data: {"choices":[{"delta":{"content":"墟"}}]}

data: [DONE]
```

前端用 `response.body.getReader()` 读取字节流，`TextDecoder` 解码，按 `\n` 分割处理每一行。`data: [DONE]` 标志流结束。

### JWT（JSON Web Token）认证流程

```
登录 → 服务端生成 JWT（header.payload.signature）
     payload = {"sub": "user_uuid", "exp": 时间戳}
     signature = HMAC-SHA256(header+payload, JWT_SECRET)

请求时 → Authorization: Bearer <token>
        → 服务端用 JWT_SECRET 验签
        → 验证 exp 未过期
        → 从 payload 取 user_id 查数据库
```

无状态：服务端不存储 token，任何时候都能用 SECRET 验证有效性，适合分布式部署。

---

## 9. 扩展与修改指南

### 修改 AI 模型

`backend/routers/ai.py`：
```python
QWEN_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions"
# 修改 json 中的 model 字段：
json={"model": "qwen-plus", ...}  # 改为 qwen-max / qwen-turbo 等
```

### 添加新属性

1. `frontend/index.html` → `gs.stats` 添加新属性键
2. `frontend/index.html` → `updateStatUI()` 添加 DOM 更新
3. `frontend/index.html` → System Prompt 中的属性列表添加新项
4. `frontend/index.html` → `applyCommands()` 添加新 command 处理
5. `backend/schemas.py` → `SaveCreate.stats` 的默认值添加新键

### 添加新的结局类型

`frontend/index.html`：
```javascript
// showEndingScreen 函数
const titles = {
    death: '故事终结',
    victory: '故事圆满',
    open: '故事落幕',
    yourNewType: '你的标题'    // ← 添加
}
// CSS 中添加对应的 .ending-yourNewType 样式
```

System Prompt 中同步添加新结局类型的说明。

### 修改图片生成触发条件

目前只在特殊事件时生成（`eventTurn`），如需每回合生成：

```javascript
// callGM() 中
// 当前：
if (eventTurn && imgWrap) { fetchImage() }

// 改为每回合：
if (imgWrap) { fetchImage() }
// 同时移除 if (eventTurn) 对 imgWrap 的限制
```

### 增加新的 AI 指令类型

1. `backend/schemas.py` → 无需改动（commands 是 JSON，自由结构）
2. `frontend/index.html` → `applyCommands()` 添加新 case：
```javascript
function applyCommands(commands) {
    ...
    if (c.WEATHER) { updateWeatherUI(c.WEATHER); addLog(`天气：${c.WEATHER}`) }
    ...
}
```
3. System Prompt 中的 commands 说明添加新字段文档

---

*版本 v0.11 · 2026-05*
