# Root-level env for Docker Compose variable substitution
# Copy to .env and set a strong password

# Used by docker-compose.yml for the PostgreSQL container and backend DATABASE_URL override
DB_PASSWORD=CHANGE_ME_strong_password
